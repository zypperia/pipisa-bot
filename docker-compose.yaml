volumes:
  redis-data: {}

services:
  redis:
    image: redis
    container_name: redis
    restart: unless-stopped
    ports:
      - 6379:6379
    volumes:
      - redis-data:/data
  bot:
    image: ghcr.io/zypperia/pipisa-bot:master
    env_file: .env
    restart: unless-stopped
